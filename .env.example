# ============================================
# ForeverUsInLove - Environment Variables
# ============================================
# 
# INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Fill in the actual values for your environment
# 3. Never commit .env to version control
#
# ============================================

# Environment Configuration
ENVIRONMENT=development
# Options: development, staging, production

# API Configuration
API_BASE_URL=https://api.example.com/v1
API_KEY=your-api-key-here
API_TIMEOUT=30000
# Timeout in milliseconds (30000 = 30 seconds)

# AWS Configuration
AWS_REGION=us-east-1
# Available regions: us-east-1, us-west-2, eu-west-1, etc.

AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# AWS S3 - For image and file storage
AWS_S3_BUCKET_NAME=forever-us-in-love-bucket
# Bucket for storing user profile images, ID verification photos, etc.

# AWS Cognito - For user authentication
AWS_COGNITO_USER_POOL_ID=us-east-1_xxxxxxxxx
AWS_COGNITO_CLIENT_ID=your-cognito-client-id
# Cognito User Pool for authentication and user management

# Firebase Configuration (Optional - if using Firebase services)
FIREBASE_API_KEY=your-firebase-api-key
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_MESSAGING_SENDER_ID=your-sender-id
FIREBASE_APP_ID=your-firebase-app-id
# Firebase for push notifications and analytics

# OAuth Configuration
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# For Google Sign-In integration

FACEBOOK_APP_ID=your-facebook-app-id
# For Facebook Login integration

# Feature Flags
ENABLE_LOGGING=true
# Set to true in development, false in production

ENABLE_ANALYTICS=false
# Enable analytics tracking (Firebase, Amplitude, etc.)

# Face Recognition / Identity Verification Service (if using external service)
# FACE_RECOGNITION_API_URL=https://api.face-recognition-service.com
# FACE_RECOGNITION_API_KEY=your-face-recognition-api-key

# SMS Service (for OTP verification)
# SMS_SERVICE_API_URL=https://api.sms-service.com
# SMS_SERVICE_API_KEY=your-sms-service-api-key

# ============================================
# AWS Services Setup Guide
# ============================================
#
# 1. AWS S3 Setup:
#    - Create S3 bucket for storing images
#    - Enable CORS for mobile app access
#    - Set up lifecycle policies for old images
#    - Configure bucket permissions (private with signed URLs)
#
# 2. AWS Cognito Setup:
#    - Create User Pool for authentication
#    - Configure password policies (min 8 chars, uppercase, lowercase, number)
#    - Enable MFA (optional)
#    - Set up OAuth providers (Google, Facebook)
#    - Configure app client with appropriate flows
#
# 3. AWS IAM Setup:
#    - Create IAM user for mobile app backend
#    - Attach policies:
#      * AmazonS3FullAccess (or custom policy for specific bucket)
#      * AmazonCognitoPowerUser
#    - Generate Access Key ID and Secret Access Key
#
# 4. AWS Lambda (Optional):
#    - For Face ID verification processing
#    - For image moderation and validation
#    - For sending SMS/Email OTPs
#
# 5. AWS CloudFront (Optional):
#    - CDN for faster image delivery
#    - HTTPS endpoints for secure access
#
# ============================================
